/usr/lib/R/bin/R
library(devtools)
library(reshape2)
# Github (dev version)
devtools::install_github('msperlin/yfR')
library(ggplot2)
library(yfR)
library(dplyr)
/usr/lib/R/bin/R
usr/lib/R/bin/R
.libPaths(),
.libPaths()
.libPaths?
?
}
libPaths?
()
library(ggplot2)
library(yfR)
library(dplyr)
libPaths(ggplot2)
libPaths("ggplot2")
.libPaths("ggplot2")
.libPaths()
.Library.site()
.Library.site
.Library
.libPaths()
.libPaths()[-1]
library("dplyr", lib.loc=.libPaths()[-1])
.libPaths()
?library
Sys.getenv("R_LIBS_USER")
library("dplyr", lib.loc=Sys.getenv("R_LIBS_USER"))
Sys.getenv("R_LIBS_USER")
library(quantmod)
?myGetSymbols
?getSymbols.yahoo
input.tickers.df$ticker
input.tickers.df = data.frame(ticker=c("ROG.SW",
"NESN.SW",
"NOVN.SW",
"^SSMI"),
friendly.name=c("Roche",
"Nestle",
"Novartis",
"SMI"))
daily.returns.data.long <- getSymbols.yahoo(
Symbols      = as.character(input.tickers.df$ticker),
from   = start.date,
to     = end.date,
periodicity    = "daily")
start.date=as.Date("2015-01-01")
end.date=as.Date("2022-01-13")
# load data via BatchGetSymbols
daily.returns.data.long <- getSymbols.yahoo(
Symbols      = as.character(input.tickers.df$ticker),
from   = start.date,
to     = end.date,
periodicity    = "daily")
# type_return  = "log",
# cache_folder
daily.returns.data.long <- getSymbols.yahoo(
Symbols      = as.character(input.tickers.df$ticker),
from   = start.date,
to     = end.date,
periodicity    = "daily")
